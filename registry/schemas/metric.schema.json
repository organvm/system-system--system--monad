{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "System Metric Observation",
  "description": "A timestamped observation or time-series rollup.",
  "type": "object",
  "properties": {
    "uid": {
      "type": "string",
      "pattern": "^met_[a-zA-Z0-9]{5}$"
    },
    "metric_name": {
      "type": "string",
      "description": "Name of the variable being observed (e.g., active_task_count)."
    },
    "observation_type": {
      "type": "string",
      "enum": ["GAUGE", "COUNTER", "DELTA", "ROLLING"],
      "description": "The category of the metric determining how it should be aggregated."
    },
    "value": {
      "type": "number",
      "description": "The observed value."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "When the observation was recorded."
    },
    "related_uid": {
      "type": "string",
      "description": "Optional: the UID of the entity or context this metric describes."
    }
  },
  "required": ["uid", "metric_name", "observation_type", "value", "timestamp"]
}
